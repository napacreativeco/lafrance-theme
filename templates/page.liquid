<div class="default-page">
    <h1>{{ page.title }}</h1>

    <div class="default-content {% if template.name == 'faq' %} faq-page {% endif %}">
        {{ page.content }}
    </div>
</div>


